/**
 * Execution status.
 */
export enum ExecutionStatus {
  /** Test passed. */
  PASSED,

  /** Failed test. */
  FAILED,

  /** Work in progress. */
  WORK_IN_PROGRESS,

  /** Blocked. */
  BLOCKED
}
